# Generated by the harness: the fixture's schema/ is committed, everything the
# tool writes into the mounted folder is not.
project/database/rollouts/*.toml
project/database/snapshots/*.json
project/database/setup.surql
